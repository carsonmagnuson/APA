import argparse

def parse(input: str) -> argparse.Namespace:
  """
  """
  parser = argparse.ArgumentParser(description="A not so simple command line tool for running an optimized positive selection analysis on a gene of interest.")
  
